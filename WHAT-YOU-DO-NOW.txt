SPY Render OOM fix + go A+E | 2026-06-04
========================================

RENDER MEMORY (on our side — deploy v5.5.11)
  Cause: large /exercise/burst (e.g. count=100) on free tier ~512MB
  Fix: BURST_MAX_COUNT=10, max 2 chase tasks, slim API response
  Live MACD: brief 502 if Render restarts — avoid BURST-PAPER-100 during session
  Doc: docs\RENDER-MEMORY.md

IMPLEMENTED (repo — push + Manual Deploy)
  go A+E — fill chase env + PAPER-FILL-TEST + PAPER-PNL-AUDIT
  Render guard — main.py + render.yaml v5.5.11

YOUR STEPS
  1. DONE — git push (5b74119). YOU: Render Manual Deploy today
     → ideally OUTSIDE the next MACD window (or a gap you accept)
  2. Confirm /health → version 5.5.11 (still 5.5.10 until deploy)
  3. UNTIL 5.5.11 LIVE: no large bursts — most likely OOM trigger
  4. After 5.5.11: still avoid BURST-PAPER-100 during live MACD
  5. Unset REDUNDANT_BURST_EACH_CYCLE on PC if set
  6. Off-hours: PUSH-PAPER-FILL-TEST.bat + PAPER-PNL-AUDIT.bat --try-entry

NOT STARTED
  Track D (0dte hold) — reply go D after fills look good

BRIDGE
  https://spy-options-bridge.onrender.com/health
